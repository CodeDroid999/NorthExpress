import React from 'react'

const RedDot = () => {
  const dotStyle = {
    width: '10px',
    height: '10px',
    backgroundColor: 'red',
    borderRadius: '50%',
  }

  return <div style={dotStyle}></div>
}

export default RedDot
